doctype html
html(lang='en')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible' content='IE=edge')
    meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
    meta(name='description' content='')
    meta(name='author' content='')
    title Đăng nhập
    // Custom fonts for this template
    link(href='https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i' rel='stylesheet')
    // Custom styles for this template
    link(href='/bootstrap/css/bootstrap.min.css' rel='stylesheet')
    link(rel='stylesheet' href='/css/login.css')
    script(src="/javascripts/login.js")
  body.bg-gradient-primary
    .container
      // Outer Row
      .row.justify-content-center
        .col-sm-5
          .card.o-hidden.border-0.shadow-lg.my-5
            .card-body.p-0
              .p-5
                .text-center
                  h1.h4.text-gray-900.mb-4 Đăng Nhập!
                form.user#form-login(action="/auth/login" method="POST")
                  .form-group
                    input#role(type="hidden" name="role" value="0")
                    input#inputEmail.form-control.form-control-user(type='email' aria-describedby='emailHelp' placeholder='Nhập email của bạn ...' name="email" required)
                  .form-group
                    input#inputPassword.form-control.form-control-user(type='password' placeholder='Nhập mật khẩu ...' autocomplete="true" name="password" required)
                  .form-group
                    .custom-control.custom-checkbox.small
                      input#customCheck.custom-control-input(type='checkbox')
                      label.custom-control-label(for='customCheck') Nhớ mật khẩu
                  button.btn.btn-primary.btn-user.btn-block(type='submit')
                    | Đăng Nhập
                  hr
                  button.btn.btn-facebook.btn-user.btn-block
                    |  Đăng nhập với quyền Giảng Viên
                  button.btn.btn-google.btn-user.btn-block
                    |  Đăng nhập với quyền Admin
                hr
                .text-center
                  a.small(href='/auth/forgot-password') Quên mật khẩu?
                .text-center
                  a.small(href='/auth/register') Tạo tài khoản!
